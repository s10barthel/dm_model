[pytest]
# Keep discovery within the test suite.  Walking the repository root currently
# reaches a damaged generated Parquet artifact (WinError 1392) before pytest
# can filter it as a non-test file.
testpaths =
    tests
